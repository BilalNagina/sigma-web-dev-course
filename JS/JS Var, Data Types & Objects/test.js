// Question one:
// var a = "bilal"
// a = a + 10
// console.log(a)
// Output: "bilal10"
// Explanation: In the above code, we have declared a variable a with a string value "bilal". Then we have added 10 to the string value. In JavaScript, when we add a number to a string, it concatenates the number to the string. So the output will be "bilal10".

// Question two:
// console.log(typeof a)
// Output: string

// Question three:
const a = {
  name: "bilal",
  age: 20,
};
// a = 10
// output: TypeError: It has already been declared

// Question four:
a.father = "bilal ahmed"
console.log(a)
// output: { name: 'bilal', age: 20, father: 'bilal ahmed' }
// ex

// Question five:
const dictionary = {
    name: "bilal",
    right: "true",
    wrong: "false",
    hate: "dislike",
    love: "like",
    };
    console.log(dictionary);
// explanation: In the above code, we have declared a constant dictionary with key-value pairs. We have added some key-value pairs to the dictionary object. Then we have printed the dictionary object using console.log(). The output will be the dictionary object with all the key-value pairs.